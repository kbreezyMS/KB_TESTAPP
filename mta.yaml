ID: KB_TESTAPP
_schema-version: '2.1'
version: 0.0.1
modules:
  - name: web
    type: html5
    path: web
    requires:
      - name: UAA-Service
resources:
  - name: UAA-Service
    type: com.sap.xs.uaa-space
    parameters:
      config-path: /xs-security.json
